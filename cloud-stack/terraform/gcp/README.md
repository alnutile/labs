# Google Cloud lab

The Google Cloud implementation will preserve the application contract in `docs/architecture.md` while using Google Cloud-native services. It will be added after the AWS and Azure slices are understood.

