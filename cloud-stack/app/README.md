# Application boundary

The application code will be introduced here after the infrastructure contract is stable. The Supabase starter remains the reference implementation for auth, RLS, storage, realtime, webhooks, and Edge Functions.

Keep application secrets out of this directory. Use `.env.example` for names only and cloud secret managers for deployed values.

